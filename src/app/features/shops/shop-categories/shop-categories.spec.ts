import { ShopCategories } from './shop-categories';

describe('ShopCategories', () => {
  it('should create', () => {
    const component = new ShopCategories();
    expect(component).toBeTruthy();
  });
});
