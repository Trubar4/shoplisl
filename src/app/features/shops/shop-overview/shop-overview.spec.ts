import { ShopOverview } from './shop-overview';

describe('ShopOverview', () => {
  it('should create', () => {
    const component = new ShopOverview();
    expect(component).toBeTruthy();
  });
});
